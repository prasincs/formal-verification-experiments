# Cargo configuration for seL4 Microkit

[env]
RUST_TARGET_PATH = { value = "support/targets", relative = true }

[unstable]
unstable-options = true
build-std = ["core", "alloc", "compiler_builtins"]
build-std-features = ["compiler-builtins-mem"]
